"""Fehlerinjektor — erzeugt ``df_raw_dirty`` und den Ground Truth.

Entsteht in Phase 4, nach dem Git-Tag ``freeze-regelkatalog``. Darf nicht aus
``src.rules`` oder ``src.generator`` importieren.
"""
